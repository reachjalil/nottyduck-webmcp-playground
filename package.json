{
  "name": "nottyduck-webmcp-playground",
  "version": "1.0.0",
  "private": false,
  "description": "A tiny local-first WebMCP playground where people and agents make mischief together.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check public/core.js && node --check public/app.js && node --check public/webmcp.js && node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
